#!/bin/sh
# Прогон проверок скетча на обычном компьютере: FastLED, EEPROM и Arduino
# подменены заглушками, лента симулируется массивом. Проверяются диапазоны
# диодов и порядок шагов по каждому условию ТЗ.
set -e
cd "$(dirname "$0")"
g++ -std=c++11 -I. main.cpp runtime.cpp -o sim
./sim
